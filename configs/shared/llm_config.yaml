# configs/shared/llm_config.yaml (CẬP NHẬT HOÀN CHỈNH)

# Cấu hình chính được sử dụng bởi LLMFactory (LLMServiceConfig)
LLM_SERVICE:
  # Chi phí cao, chất lượng cao, có Retry/Fallback (Primary LLM)
  primary:
    type: "openai"
    # 🚨 CẬP NHẬT: Các tham số nằm trực tiếp ở đây, không lồng trong 'config'
    model_name: "gpt-4o"
    api_key_secret_name: "OPENAI_API_KEY"
    temperature: 0.7
    max_tokens: 1500
    max_retries: 5 # Có thể được chuyển vào logic LLM Wrapper
    retry_delay_seconds: 4 # Có thể được chuyển vào logic LLM Wrapper

  # Chi phí thấp, dùng khi Primary thất bại (Fallback LLM)
  fallback:
    type: "huggingface"
    # 🚨 CẬP NHẬT: Các tham số nằm trực tiếp ở đây
    model_name: "llama-3-8b-instruct"
    model_path: "/models/llama-3-8b"
    device: "cuda"

# Cấu hình mô hình Embedding (thường là mô hình khác)
EMBEDDING_MODEL:
  type: "openai"
  model_name: "text-embedding-3-small"
  # 🚨 BỔ SUNG: Cần API key nếu embedding model không dùng chung secret
  api_key_secret_name: "OPENAI_EMBEDDING_API_KEY"
