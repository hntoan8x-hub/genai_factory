# ----------------------------------------------------
# 🔹 K8S SECRET: Lưu trữ các giá trị nhạy cảm (API Keys, Tokens)
# ----------------------------------------------------
apiVersion: v1
kind: Secret
metadata:
  name: genai-factory-secrets
type: Opaque
data:
  # Lấy từ file .env hoặc Secret Manager (BASE64 ENCODED)
  OPENAI_API_KEY: <base64_encoded_openai_key>
  PINECONE_API_KEY: <base64_encoded_pinecone_key>
  MLFLOW_TRACKING_URI: <base64_encoded_mlflow_uri>
  REDIS_CONNECTION_STRING: <base64_encoded_redis_uri>
---
# ----------------------------------------------------
# 🔹 K8S CONFIGMAP: Lưu trữ các giá trị cấu hình không nhạy cảm (Paths, URLs)
# ----------------------------------------------------
apiVersion: v1
kind: ConfigMap
metadata:
  name: genai-factory-configs
data:
  # Tên môi trường Pinecone/Weaviate
  PINECONE_ENVIRONMENT: "gcp-starter-region"

  # Đường dẫn cấu hình (đã được copy vào Docker Image)
  INGESTION_CONFIG_PATH: "configs/ingestion/default_ingestion.yaml"
  FEATURE_STORE_CONFIG_PATH: "configs/feature_store/default_config.yaml"
