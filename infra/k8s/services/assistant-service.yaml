apiVersion: v1
kind: Service
metadata:
name: genai-assistant-service
spec:
selector:
app: genai-assistant
ports:
  - protocol: TCP
port: 80
targetPort: 8000
type: ClusterIP # Use LoadBalancer for external access
